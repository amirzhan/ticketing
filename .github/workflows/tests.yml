name: Tests
on:
  pull_request
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: cd auth && echo -e "//npm.pkg.github.com/:_authToken=${‌{secrets.GITHUB_TOKEN}}\n" > .npmrc && npm install && npm run test:ci